samples:

sample2-multi-handlers
sample3-reg-new-session-fn

sample with login/logout (states)
sample with meta-data expires
sample with own reserved words
sample with several $HttpCacheIndicator
sample with server side cache
sample with socket-io
sample with $getFunction
sample with $authorizeFunction





to release:

unit tests:
attachService
injectParamPrefix

future:
add partial match switch
add that verbose that prints out command structures via command line
